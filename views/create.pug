extends layout.pug

block title
	title Create new poll

block content
	h1 Create a new poll
	form(
		action="/create"
		method="post"
		onsubmit="return verifyNewPollSubmit()"
	)
		div
			label Question:
			input(
				class="input-field"
				type="text"
				name="title"
				id="question-field"
				autofocus
			)
		div(id="options-container")
			div(id="model-create-field")
				label Option:
				input(
					type="text"
					name="options"
					class="option-field input-field"
				)
			div
				label Option:
				input(
					type="text"
					name="options"
					class="option-field input-field"
				)
		div
			input(
				type="button"
				class="button"
				value="Add another option"
				id="add-button"
				onclick="addCreateField()"
			)
			input(
				type="submit"
				value="Sumbit"
				class="button"
			)
		div(id="message")
			| #{message}